config:
  target: "http://localhost:3000" # Replace with your application's URL
  phases:
    - duration: 10
      arrivalRate: 8 # 10 requests per second
scenarios:
  - flow:
      - get:
          url: "/orders" # Replace with your API endpoint